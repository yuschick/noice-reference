import { AppSessionStorageKeys } from './types';

export const defaultSessionData: AppSessionStorageKeys = {
  'gameStream.active.isSoloPlay': false,
  'gameStream.active.streamId': '',
  'platform.mobileAppBanner.isShown': true,
};
