import { GameSessionDataKeys } from '@noice-com/card-game';

// @todo: Change to interface that extends GameSessionDataKeys once
// we need to add more keys to the session storage.
export type AppSessionStorageKeys = GameSessionDataKeys;
