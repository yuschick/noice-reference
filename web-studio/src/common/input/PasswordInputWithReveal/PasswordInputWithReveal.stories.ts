import { PasswordInputWithReveal } from './PasswordInputWithReveal';

export default {
  title: 'PasswordInputWithReveal',
  component: PasswordInputWithReveal,
};

export const Default = {
  args: {},
};
